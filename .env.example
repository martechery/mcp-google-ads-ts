# Copy to .env and fill in values. Do NOT commit .env.

# Notes
# - Most tools use Application Default Credentials (ADC). Preferred path is gcloud:
#   gcloud auth application-default login --scopes=https://www.googleapis.com/auth/adwords
# - Never commit real tokens or ADC files.

# Required Google Ads developer token
GOOGLE_ADS_DEVELOPER_TOKEN=
# Required. Your Google Ads Developer Token (string). Obtain from Google Ads UI.
# Docs: https://developers.google.com/google-ads/api/docs/get-started/dev-token

# Optional: Quota project used for billing with ADC (helps avoid 403s)
# Typically your active gcloud project ID
GOOGLE_ADS_QUOTA_PROJECT_ID=
# Optional. GCP project ID for quota/billing with ADC. Helps avoid 403s due to missing quota.
# Typically the active gcloud project (e.g., my-gcp-project).

# Auth mode (default: adc)
GOOGLE_ADS_AUTH_TYPE=adc
# Auth type. Allowed values: adc (default). Other modes are not supported here.

# Optional: Direct access token for dev/testing (bypasses ADC)
# GOOGLE_ADS_ACCESS_TOKEN=
# Optional. Direct bearer access token for dev/testing only. When set, bypasses ADC.

# Optional: Manager and account IDs (10 digits, no dashes)
# GOOGLE_ADS_MANAGER_ACCOUNT_ID=
# Optional. Manager (MCC) account ID. 10 digits, numbers only (no dashes/spaces).
# Example: 1234567890

# GOOGLE_ADS_ACCOUNT_ID=
# Optional. Default customer account ID. 10 digits, numbers only (no dashes/spaces).
# Example: 1234567890

# GOOGLE_ADS_CUSTOMER_ID=
# Optional alias for GOOGLE_ADS_ACCOUNT_ID used in some clients. Same format.

# Optional: Google Ads API version (default v19). Example: v21
# GOOGLE_ADS_API_VERSION=v21
# Optional. Ads API version string (e.g., v19, v20, v21). Defaults to v19 if unset.

# Optional: OAuth client credentials (Desktop app) to create local ADC via manage_auth { action: "oauth_login" }
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# Optional. Desktop OAuth client to create a local ADC file via the manage_auth tool
# (device flow). Use only if you cannot use gcloud. Do not commit real values.

# GOOGLE_APPLICATION_CREDENTIALS=
# Optional. Absolute path to an ADC authorized_user JSON. If set, overrides well-known ADC.
# Example: /Users/me/.config/gcloud/application_default_credentials.json
