{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "..",
    "types": ["node", "vitest"]
  },
  "include": ["unit/**/*.ts", "utils/**/*.ts", "setup.env.ts"],
  "exclude": ["integration/**", "../dist", "../node_modules"]
}
